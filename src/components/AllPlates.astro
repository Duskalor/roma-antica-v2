---
import Plates from './Plates.astro';
import { pizzas } from '@lib/pizzas';
import { pastas } from '@lib/pastas';
import { vinos } from '@lib/vinos';
---

<Plates products={pizzas.slice(0, 3)} title='PIZZAS' />
<Plates products={pastas.slice(0, 3)} title='PASTAS' />
<Plates products={vinos.slice(0, 3)} title='VINOS' />
